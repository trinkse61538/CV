# Step 5 — Knowledge Graph + Flag Language Switcher

## Added
- `knowledge-graph.jsonld`: central graph linking the Person, WebSite, project collections, article collections, projects, articles and EN/VI translations.
- `assets/flags/english.svg` and `assets/flags/vietnam.svg`.
- Compact page-level JSON-LD relationship graph on every bilingual page.
- Flag-based language selector with accessible labels.

## Hidden files
The patch contains no hidden files. The full repository keeps the original `.nojekyll` and `.gitkeep` files unchanged.
